# Copy to .env and fill in. Get a key at https://www.firecrawl.dev (Account -> API Keys)
FIRECRAWL_API_KEY=fc-...
# Safety cap: abort a run that would scrape more pages than this
MAX_PAGES_PER_RUN=200
